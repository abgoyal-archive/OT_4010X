
#include "bit_util.h"


uint32 BitUtil::bitValue
(
    octet* buff,
    uint32 offset,
    uint32 len
)
{
    OSCL_UNUSED_ARG(buff);
    OSCL_UNUSED_ARG(offset);
    OSCL_UNUSED_ARG(len);

    uint32 retVal = 0;

    return retVal;
}


bool BitUtil::bitCopy
(
    octet* src,
    uint32 src_offset,
    uint32 bitLen,
    octet** output,
    uint32& byteLenOut
)
{
    OSCL_UNUSED_ARG(src);
    OSCL_UNUSED_ARG(src_offset);
    OSCL_UNUSED_ARG(bitLen);
    OSCL_UNUSED_ARG(output);
    OSCL_UNUSED_ARG(byteLenOut);

    bool bRet = true;

    return bRet;
}


