

#ifndef PV_OSCL_H
#define PV_OSCL_H
#include <string.h>

char* PV_strstr(char *src, const char key);
int PV_atoi(char *buf, char new_format);
int countNumbers(char *buff);
void stringBreaker(char *buff);

#endif
